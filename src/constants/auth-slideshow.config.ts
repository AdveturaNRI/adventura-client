import { type ImageSourcePropType } from 'react-native';

export const AUTH_SLIDESHOW_IMAGES: ImageSourcePropType[] = [
  require('../../assets/auth-slideshow/back1.jpeg'),
];

export const AUTH_SLIDESHOW_INTERVAL_MS = 5500;

export const AUTH_SLIDESHOW_FADE_MS = 900;
